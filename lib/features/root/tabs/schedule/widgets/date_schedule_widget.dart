import 'package:flutter/material.dart';

class DateTScheduleWidget extends StatelessWidget {
  const DateTScheduleWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}